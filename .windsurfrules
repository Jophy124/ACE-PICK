# ACE PICK 项目 AI 交互规则 (Project Specific Rules)

## 品牌视觉规范 (Brand Aesthetics)
- **品牌名称**: ACE PICK (严禁写错为 Ace Pickleball 或其他)
- **配色方案**: 核心为背景黑 (#0a0a0c)，动效与点缀色为霓虹黄绿 (#CCFF00) 和赛博青 (#00F0FF)。
- **设计风格**: 玻璃拟物化 (Glassmorphism)、暗黑现代运动风。

## 技术实现关键点 (Tech Stack)
- **Logo 渲染**: 项目目前采用 **PDF.js** 动态将 `ACE PICK logo.pdf` 渲染至 Canvas。该逻辑依赖 `pdf_base64.js` 中的 Base64 字符串以绕过本地 CORS 限制。
- **动效库**: 核心动效使用 **GSAP** 处理入场和滚动观察。
- **自定义交互**: 包含鼠标发光追踪器 (.cursor-glow) 和首屏 3D 视角跟随。

## 交互语言要求
- 所有的用户反馈和 Task 更新必须使用**简体中文**。
- 代码注释建议使用中文，以便品牌方 owner 阅读。

## 期末论文写作全局规则 (Term Paper Writing Rules)
- **论文必须包含实证研究（计量分析）部分**，纯理论论述不合格。
- **标准实证流程**：文献综述 → 研究假设 → 数据说明 → 模型构建 → 回归结果 → 稳健性检验 → 结论。
- **推荐工具**：优先使用 Stata 或 Python (statsmodels) 进行回归分析。
- **模型参考**：OLS / 面板数据固定效应 (FE) / 双重差分 (DID) / 工具变量 (IV) 根据研究情境选择。
- **结果呈现**：回归系数表必须包含系数、标准误、t值/z值、显著性星号（***p<0.01, **p<0.05, *p<0.1）。
- **数据来源**：优先使用 CSMAR、Wind、国家统计局、世界银行等权威数据库，并在文中注明来源。
- **稳健性检验**：每个核心结论必须配备至少一种稳健性验证（替换变量、子样本、PSM等）。
